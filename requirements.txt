setuptools>=49.0.0,<=65.5.0  # Allow a range that includes versions compatible with other dependencies
wheel>=0.36.0,<0.40.0  # Relax constraint to allow minor updates
gym>=0.21.0,<0.22.0  # Preserve avoidance of breaking changes in 0.22
numpy>=1.19.0,<1.24.0  # Allow minor version updates for broader compatibility
boolean.py>=3.8,<4.1  # Relax to allow minor updates
networkx>=2.5,<2.9  # Allow minor version updates
pyyaml>=5.4,<7.0  # Allow for newer versions, avoiding breaking changes
matplotlib>=3.2.0,<3.7.0  # Broaden to include more versions, avoiding very latest if problematic
plotly>=4.9.0,<5.0.0  # Avoid major updates that might introduce breaking changes
tabulate>=0.8.0,<0.9.0  # Minor updates should be non-breaking
ordered_set>=4.0.0,<4.1.0  # Keep within a safe update range
progressbar2>=3.50.0,<3.52.0  # Allow minor updates
decorator>=4.3,<5.0  # Avoid major version updates that could break compatibility
zipp>=3.0.0,<4.0.0  # Allow some flexibility in minor versions
ipython>=7.0.0,<9.0.0  # Broaden to include a range of compatible versions
